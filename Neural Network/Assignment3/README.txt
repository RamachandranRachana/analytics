The code implements Hebbian learning rules

Input: mnist data

Output: Button to choose one of the three Hebbian rules
Filtered Learning, Delta & Unsupervised Hebb

Adjust weight button applies the appropriate Hebbian rule


Error rate is plotted after each epoch(pass over all the training samples) on graph.
Error-rate graph displays up to 1000 epochs.